package test

atomic init(p, i) {p = i;}

atomic inc(p) {p += 3;}

atomic print(p) {printf("%d\n", p);}

main() {
	a = [];
	init(a[1], 1);
	init(a[2], 1);
	par for i = 3..20 {
		init(a[i], a[i-1] + a[i - 2]);
		//inc(a[i]);
		(a[i] > 10) =>
			print(a[i]);		
	}
}
